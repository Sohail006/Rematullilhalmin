services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: asm
      POSTGRES_PASSWORD: asm
      POSTGRES_DB: asm
    ports:
      - "5432:5432"
    volumes:
      - asm_pg_data:/var/lib/postgresql/data

volumes:
  asm_pg_data:
